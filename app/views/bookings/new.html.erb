<h1>Booking Page<h1>
<%= @listing.title %>
<a><%= link_to 'Back to listing', listing_path(@listing) %></a>

<%# Form currently not working %>

<%= simple_form_for [@booking, @listing] do |f| %>
  <%= f.input :user_id %>
  <%= f.input :listing_id %>
  <%= f.text_field :booking_date,
    data: {
      controller: "flatpickr",
      flatpickr_date_format: "Y-m-d",
      flatpickr_min_date: Time.zone.now
    } %>
  <%= f.submit %>
<% end %>

<%= form_with model: Booking.new, authenticity_token: true do |f| %>

<% end %>
