<%= simple_form_for(@listing, :html => { :class => 'form-horizontal' }) do |f| %>
<div class="form-inputs">
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :price_per_day %>
  <%= f.submit class: 'btn' %>
</div>
    <% end %>
